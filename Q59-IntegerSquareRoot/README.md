# Find the Integer Square Root of a Number

Given an integer `N`, the integer square root `s` of `N` is defined as:

> `s` \* `s` <= `N` < `(s+1)` \* `(s+1)`

Write code to find the square root of an integer.
